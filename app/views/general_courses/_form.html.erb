<%= form_with(model: general_course, local: true) do |form| %>
  <% if general_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(general_course.errors.count, "error") %> prohibited this general_course from being saved:</h2>

      <ul>
        <% general_course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_code %>
    <%= form.text_field :course_code %>
  </div>

  <div class="field">
    <%= form.label :course_title %>
    <%= form.text_field :course_title %>
  </div>

  <div class="field">
    <%= form.label :course_description %>
    <%= form.text_field :course_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
