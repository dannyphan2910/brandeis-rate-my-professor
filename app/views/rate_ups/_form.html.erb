<%= form_with(model: rate_up, local: true) do |form| %>
  <% if rate_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rate_up.errors.count, "error") %> prohibited this rate_up from being saved:</h2>

      <ul>
        <% rate_up.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :review_id %>
    <%= form.number_field :review_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
