<%= form_with(model: review_form, local: true) do |form| %>
  <% if review_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_form.errors.count, "error") %> prohibited this review_form from being saved:</h2>

      <ul>
        <% review_form.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_field :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
